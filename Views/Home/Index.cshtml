@{
    ViewData["Title"] = "Home Page";
    @using System.Diagnostics;
    @using Microsoft.AspNetCore.Mvc;
    @using OpenQA.Selenium.Chrome;
    @using OpenQA.Selenium;
    @using System.Net;
    @using System.Text;
    @using CEL_MAI_SITE.Models;
    @using CEL_MAI_SITE.Controllers;
    @using CsvHelper;
    @model List<Coin>
}


<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    
    <table class="table table-hover">
        <thead>
            <tr>
            <th>Coin</th>
            <th>Price</th>
            <th>Hyperlink</th>
            </tr>
        </thead>
        
        <tbody>
        <tr>
            <td colspan="@ViewBag.NbColumns">
                     <tr>
                        <td>@{
                                using (var path = new StreamReader("D:\\#####C#\\CEL_MAI_SITE\\links.csv"))
                                using (var csv = new CsvReader(path, System.Globalization.CultureInfo.InvariantCulture))
                                {
                                    var records = csv.GetRecords<Coin>();
                                }
                            }</td>
                         <td>
                            @{
                                foreach(var coin in Model)
                                {
                                    @coin.names
                                }
                            }
                         </td>
                         <td></td>
                     </tr>   
        <tr>
            
            <td class="someCssStyle">Name</td>
            
        </tr>    
        </tbody>
    </table>
</div>
